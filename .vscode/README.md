# VS Code workspace

This folder contains a lightweight VS Code setup for working in the same
project folder that Codex uses.

Included:

- `settings.json`: editor and search defaults
- `extensions.json`: recommended extensions
- `tasks.json`: simple starter tasks

Open this project folder in VS Code:

`C:\Users\leona\OneDrive\Documentos\Workspace\Xadrez`
